{
  "name": "jazz-browser-media-images",
  "version": "0.8.21",
  "type": "module",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "license": "MIT",
  "dependencies": {
    "@types/image-blob-reduce": "^4.1.1",
    "image-blob-reduce": "^4.1.0",
    "jazz-browser": "workspace:0.8.21",
    "jazz-tools": "workspace:0.8.21",
    "pica": "^9.0.1",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "dev": "tsup src/index.ts --format esm --watch",
    "test": "vitest --run --root ../../ --project jazz-browser-media-images",
    "test:watch": "vitest --watch --root ../../ --project jazz-browser-media-images",
    "format-and-lint": "biome check .",
    "format-and-lint:fix": "biome check . --write",
    "typecheck": "tsc --noEmit --sourceMap -p tsconfig.json",
    "build": "rm -rf ./dist && tsup src/index.ts --format esm",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@types/pica": "^9.0.4"
  }
}
