{
  "name": "jazz-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "devDependencies": {
    "@changesets/cli": "^2.26.2",
    "prettier": "^3.1.1",
    "ts-node": "^10.9.1",
    "typedoc": "^0.25.13",
    "turbo": "^1.11.2",
    "vitest": "1.5.3"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "format": "turbo format",
    "changeset": "changeset",
    "changeset-version": "changeset version",
    "release": "pnpm publish -r"
  },
  "version": "0.0.0"
}
